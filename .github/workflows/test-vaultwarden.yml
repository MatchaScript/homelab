name: test(vaultwarden)

on:
  pull_request:
    paths:
      - 'charts/vaultwarden/**'
      - '.github/workflows/test-vaultwarden.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

jobs:
  lint:
    name: helm lint + template
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          fetch-depth: 1

      - name: Set up Helm
        uses: azure/setup-helm@9bc31f4ebc9c6b171d7bfbaa5d006ae7abdb4310 # v5
        with:
          version: v3.16.2

      - name: helm lint + smoke template
        run: make -C charts/vaultwarden lint

  test-list:
    name: list test cases
    runs-on: ubuntu-24.04
    outputs:
      tests: ${{ steps.list.outputs.tests }}
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          fetch-depth: 1

      - id: list
        run: |
          cd charts/vaultwarden/test
          TESTS=$(find . -mindepth 1 -maxdepth 1 -type d -name '[0-9]*-*' -printf '%P\n' \
            | sort \
            | jq -cRn '{ include: [inputs | { test: "\(.)" }] }')
          echo "tests=${TESTS}" >> "$GITHUB_OUTPUT"
          echo "${TESTS}"

  test:
    name: ${{ matrix.test }}
    needs: [lint, test-list]
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.test-list.outputs.tests) }}
    env:
      CHAINSAW_VERSION: v0.2.15
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          fetch-depth: 1

      - name: Set up Helm
        uses: azure/setup-helm@9bc31f4ebc9c6b171d7bfbaa5d006ae7abdb4310 # v5
        with:
          version: v3.16.2

      - name: Install kubectl
        uses: azure/setup-kubectl@829323503d1be3d00ca8346e5391ca0b07a9ab0d # v5

      - name: Install kind + create cluster
        uses: helm/kind-action@ef37e7f390d99f746eb8b610417061a60e82a6cc # v1.14.0
        with:
          cluster_name: vaultwarden-test
          node_image: kindest/node:v1.34.3
          wait: 120s

      - name: Install Cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2

      - name: Install Chainsaw
        uses: kyverno/action-install-chainsaw@1223ef75bedeb59c4e7b5455463d4316e76dff01 # v0.2.15
        with:
          verify: true

      - name: Run chainsaw test
        env:
          MATRIX_TEST: ${{ matrix.test }}
          CHART_DIR: ${{ github.workspace }}/charts/vaultwarden
        run: |
          chainsaw test \
            --config charts/vaultwarden/test/chainsaw.yaml \
            "charts/vaultwarden/test/${MATRIX_TEST}"

      - name: Dump cluster state on failure
        if: failure()
        run: |
          kubectl get deployments,services,pvc,pods -A -o wide || true
          kubectl logs -l app=vaultwarden --all-containers --tail=200 -A || true
