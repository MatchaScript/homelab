apiVersion: v1
kind: Service
metadata:
  name: keycloak-http
  labels:
    {{- include "keycloak.labels" . | nindent 4 }}
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    {{- include "keycloak.selectorLabels" . | nindent 4 }}
