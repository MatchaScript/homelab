apiVersion: ceph.rook.io/v1
kind: CephObjectZoneGroup
metadata:
  name: {{ required "zoneGroup.name is required" .Values.zoneGroup.name }}
  namespace: {{ .Release.Namespace }}
spec:
  realm: {{ required "realm.name is required" .Values.realm.name }}
