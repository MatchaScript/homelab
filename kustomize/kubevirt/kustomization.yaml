apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kubevirt

resources:
  - https://github.com/kubevirt/kubevirt/releases/latest/download/kubevirt-operator.yaml
  - kubevirt-config.yaml
generatorOptions:
  disableNameSuffixHash: true
