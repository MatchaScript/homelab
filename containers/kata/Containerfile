# Artifact-only image holding the kata-containers install tree. Build context is
# the directory .github/workflows/build-kata.yml unpacked the tarballs into, one
# subdirectory per architecture.
FROM scratch

ARG TARGETARCH
COPY ${TARGETARCH}/opt/kata /opt/kata
